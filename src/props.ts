export type CheckboxProps = {
    placeholder: string;
    options: {
        label: string;
        value: string;
        checked: boolean;
        disabled: boolean;
    }[];
};

export type LinearLayoutProps = {};
